<Window x:Class="MaterialWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MaterialWindow" Height="600" Width="900" Loaded="OnLoaded" Icon="icon_schwarz - 150px - 15kb .jpg">
    <Grid MouseWheel="Grid_MouseWheel" MouseDown="Grid_MouseDown" MouseMove="Grid_MouseMove" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Viewport3D Name="viewport1" >
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="camera1" LookDirection="0,0,-1" Position="0,0,3" FieldOfView="45" />
            </Viewport3D.Camera>

            <ModelVisual3D >
                <ModelVisual3D.Content>
                    <Model3DGroup x:Name="group1">
                        <DirectionalLight Direction="0,0,-1" Color="White" />
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>

        </Viewport3D>
        <Viewport3D  Grid.Row="1">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="camera2" LookDirection="0,0,-1" Position="0,0,3" FieldOfView="45" />
            </Viewport3D.Camera>

            <ModelVisual3D >
                <ModelVisual3D.Content>
                    <Model3DGroup x:Name="group2">
                        <DirectionalLight Direction="0,0,-1" Color="White" />
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>

        </Viewport3D>
        <Viewport3D Grid.Column="1" >
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="camera3" LookDirection="0,0,-1" Position="0,0,3" FieldOfView="45" />
            </Viewport3D.Camera>

            <ModelVisual3D >
                <ModelVisual3D.Content>
                    <Model3DGroup x:Name="group3">
                        <DirectionalLight Direction="0,0,-1" Color="White" />
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>

        </Viewport3D>
        <Viewport3D Grid.Row="1" Grid.Column="1" >
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="camera4" LookDirection="0,0,-1" Position="0,0,3" FieldOfView="45" />
            </Viewport3D.Camera>

            <ModelVisual3D >
                <ModelVisual3D.Content>
                    <Model3DGroup x:Name="group4">
                        <DirectionalLight Direction="0,0,-1" Color="White" />
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>

        </Viewport3D>
        <Viewport3D Grid.Column="2" >
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="camera5" LookDirection="0,0,-1" Position="0,0,3" FieldOfView="45" />
            </Viewport3D.Camera>

            <ModelVisual3D >
                <ModelVisual3D.Content>
                    <Model3DGroup x:Name="group5">
                        <DirectionalLight Direction="0,0,-1" Color="White" />
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>

        </Viewport3D>
        <Viewport3D Grid.Row="1" Grid.Column="2" >
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="camera6" LookDirection="0,0,-1" Position="0,0,3" FieldOfView="45" />
            </Viewport3D.Camera>

            <ModelVisual3D >
                <ModelVisual3D.Content>
                    <Model3DGroup x:Name="group6">
                        <DirectionalLight Direction="0,0,-1" Color="White" />
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>

        </Viewport3D>

    </Grid>
</Window>
