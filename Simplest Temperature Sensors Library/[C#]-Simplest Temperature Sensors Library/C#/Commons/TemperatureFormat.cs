using System;
using Microsoft.SPOT;

namespace TemperatureSensorsLibrary.Commons
{
    /// <summary>
    /// Helper enum for setting temperature return format
    /// </summary>
    public enum TemperatureFormat : byte
    {
        CELSIUS,
        KELVIN
    }
}
