<UserControl x:Class="Illusion.Product.View.ProductTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
              Height="Auto" Width="Auto">
    <Border BorderThickness="0.5" BorderBrush="White" Height="40">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Product" Grid.Column="0" Style="{StaticResource ButtonStyle}" Command="{Binding ProductClickCommand}">
            </Button>
            <Button Content="Product Model" Grid.Column="1" Style="{StaticResource ButtonStyle}"  Command="{Binding ProductModelClickCommand}"/>
            <Button Content="Product Assembly" Grid.Column="2" Style="{StaticResource ButtonStyle}"  Command="{Binding ProductAssemblyClickCommand}"/>
        </Grid>
    </Border>
</UserControl>
